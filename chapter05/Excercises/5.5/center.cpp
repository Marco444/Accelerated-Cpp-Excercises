#include <iostream>
#include <string>

using std::vector;      using std::string;
using std::setw;

vector<string> center(const vector<string>& ipicture)
{
  vector<string> fpicture = frame(ipicture);
  int padding = width(ipicture);

  return 
}
